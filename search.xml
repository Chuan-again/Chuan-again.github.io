<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>Android 车载音频知识笔记 — AIDL 服务注册 / VINTF 校验 / NDK 后端 / binder 线程池</title>
      <link href="/2026/09/11/%E8%BD%A6%E8%BD%BD%E9%9F%B3%E9%A2%91%E7%9F%A5%E8%AF%86%E7%AC%94%E8%AE%B0-AIDL%E6%9C%8D%E5%8A%A1%E4%B8%8EVINTF%E6%9C%BA%E5%88%B6/"/>
      <url>/2026/09/11/%E8%BD%A6%E8%BD%BD%E9%9F%B3%E9%A2%91%E7%9F%A5%E8%AF%86%E7%AC%94%E8%AE%B0-AIDL%E6%9C%8D%E5%8A%A1%E4%B8%8EVINTF%E6%9C%BA%E5%88%B6/</url>
      
        <content type="html"><![CDATA[<p>本文整理 sa8797 平台音频链路（QTI audio-hal-ar + vendor 定制服务）中 AIDL 服务从加载、注册到校验的完整机制，覆盖 QTI AHAL 的 dlopen 加载模式、AutomotiveAudioControl 的注册结构解剖、servicemanager 的 VINTF 声明强制校验、AIDL 后端（NDK &#x2F; CPP）选型，以及 binder 线程池的建池与复用模式，最后附一份自定义服务（MyService）的集成与部署清单。</p><blockquote><p>代码库：<code>Y:\code\v6\android</code>（sa8797 平台）<br>涉及模块：<code>vendor/qcom/opensource/audio-hal-ar</code>（QTI AHAL）、<code>vendor/gwm/interfaces</code>（定制服务）、<code>frameworks/native</code>（servicemanager &#x2F; libbinder_ndk）</p></blockquote><h2 id="速查总览"><a href="#速查总览" class="headerlink" title="速查总览"></a>速查总览</h2><table><thead><tr><th>#</th><th>主题</th><th>结论</th></tr></thead><tbody><tr><td>1</td><td>QTI AHAL 加载模式</td><td><code>audiohalservice.qti</code> 启动时按板级 <code>vendor_audio_interfaces.xml</code> 逐个 <code>dlopen</code> + <code>dlsym(method)</code> 拉起各 HAL 库，所有 audio HAL 服务都在这一个进程里</td></tr><tr><td>2</td><td>AutomotiveAudioControl</td><td>整个目录经 <code>registerServices()</code> 只注册 <strong>1 个</strong>服务：<code>android.hardware.automotive.audiocontrol.IAudioControl/default</code></td></tr><tr><td>3</td><td>VINTF 声明强制校验</td><td>标了 <code>@VintfStability</code> 的接口未在 VINTF manifest 声明 → servicemanager <strong>拒绝注册</strong>（<code>EX_ILLEGAL_ARGUMENT &quot;VINTF declaration error.&quot;</code>），注意是拒绝而非警告</td></tr><tr><td>4</td><td>AIDL NDK 后端</td><td>生成基于 <code>libbinder_ndk</code> 的绑定（<code>::ndk::BnCInterface</code>、include 带 <code>aidl/</code> 前缀、模块名 <code>-ndk</code> 后缀），HAL &#x2F; vendor 服务标准选择</td></tr><tr><td>5</td><td>binder 线程池</td><td>由主程序 <code>main()</code> 统一建池（16 线程）；库代码用 <code>ABinderProcess_isThreadPoolStarted()</code> 检查后<strong>复用</strong>，不自建</td></tr><tr><td>6</td><td>部署清单</td><td>3 个 <code>.so</code> + VINTF fragment + service_contexts（+ sepolicy &#x2F; FCM），缺一不可</td></tr></tbody></table><h2 id="一、QTI-AHAL-服务加载架构（dlopen-模式）"><a href="#一、QTI-AHAL-服务加载架构（dlopen-模式）" class="headerlink" title="一、QTI AHAL 服务加载架构（dlopen 模式）"></a>一、QTI AHAL 服务加载架构（dlopen 模式）</h2><h3 id="1-1-加载链路"><a href="#1-1-加载链路" class="headerlink" title="1.1 加载链路"></a>1.1 加载链路</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">audiohalservice.qti: main()                       ← primary-hal/hal/service/Service.cpp</span><br><span class="line">  ├─ L116  ABinderProcess_setThreadPoolMaxThreadCount(16)</span><br><span class="line">  ├─ L117  ABinderProcess_startThreadPool()       ← 线程池先在主程序建好</span><br><span class="line">  ├─ L119  registerAvailableInterfaces()</span><br><span class="line">  │     └─ registerFromConfigs() → parseInterfaces()      ← ConfigManager.cpp</span><br><span class="line">  │           └─ 读取 vendor_audio_interfaces.xml</span><br><span class="line">  │                （在 audio_get_configuration_paths() 搜索路径中找，DEFAULT_NAME 常量）</span><br><span class="line">  │                 └─ registerServiceImplementation()     ← Service.cpp L31-51</span><br><span class="line">  │                       dlopen(库) + dlsym(method) + 调用</span><br><span class="line">  └─ L124  ABinderProcess_joinThreadPool()</span><br></pre></td></tr></table></figure><h3 id="1-2-板级接口配置（gen5-au）"><a href="#1-2-板级接口配置（gen5-au）" class="headerlink" title="1.2 板级接口配置（gen5_au）"></a>1.2 板级接口配置（gen5_au）</h3><p><code>configs/gen5_au/vendor_audio_interfaces_VIOSND.xml</code>：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">library</span> <span class="attr">name</span>=<span class="string">&quot;audiocontrolService&quot;</span> <span class="attr">libraryName</span>=<span class="string">&quot;libAutomotiveAudioControl.so&quot;</span></span></span><br><span class="line"><span class="tag">         <span class="attr">method</span>=<span class="string">&quot;registerServices&quot;</span> <span class="attr">mandatory</span>=<span class="string">&quot;true&quot;</span> /&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">library</span> <span class="attr">name</span>=<span class="string">&quot;audiohal-qti&quot;</span> <span class="attr">libraryName</span>=<span class="string">&quot;libaudiocorehal.qti.so&quot;</span></span></span><br><span class="line"><span class="tag">         <span class="attr">method</span>=<span class="string">&quot;registerServices&quot;</span> <span class="attr">mandatory</span>=<span class="string">&quot;true&quot;</span> /&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">library</span> <span class="attr">name</span>=<span class="string">&quot;palservice&quot;</span> <span class="attr">libraryName</span>=<span class="string">&quot;libpalipcservice.so&quot;</span> <span class="attr">...</span> /&gt;</span></span><br><span class="line">...</span><br></pre></td></tr></table></figure><p>要点：</p><ul><li>新增独立服务库 → 在此 xml 加 <code>&lt;library&gt;</code> 条目（声明库名 + 导出函数名）</li><li>挂到已有库里的服务（如 MyService 挂 ModulePrimary）→ 不需要动此 xml</li><li><code>vendor.audio.hal.stubmode</code> 属性打开时会走 <code>registerDefaultInterfaces()</code> 的兜底列表</li><li><code>mandatory=true</code> 当前不会真正 fatal（<code>gFatalIfMandatoryInterfaceMissing=false</code>，代码留有 TODO）</li></ul><h3 id="1-3-服务注册的两种接入模式"><a href="#1-3-服务注册的两种接入模式" class="headerlink" title="1.3 服务注册的两种接入模式"></a>1.3 服务注册的两种接入模式</h3><table><thead><tr><th>模式</th><th>做法</th><th>代表</th></tr></thead><tbody><tr><td>独立库</td><td>自己的 .so 导出 <code>registerServices()</code>，板级 xml 声明</td><td>AutomotiveAudioControl</td></tr><tr><td>挂载既有库</td><td>在已有 registerServices 链里加调用（QTI <code>ModulePrimary</code> 构造函数）</td><td>MyService &#x2F; NoboAudioService</td></tr></tbody></table><h2 id="二、案例：AutomotiveAudioControl-服务注册结构"><a href="#二、案例：AutomotiveAudioControl-服务注册结构" class="headerlink" title="二、案例：AutomotiveAudioControl 服务注册结构"></a>二、案例：AutomotiveAudioControl 服务注册结构</h2><blockquote><p>目录：<code>vendor/gwm/interfaces/impl/audio/AutomotiveAudioControl/default/</code><br>（<code>NoboAudioControl/</code> 是它的实现子目录）</p></blockquote><h3 id="2-1-结论：只注册-1-个-binder-服务"><a href="#2-1-结论：只注册-1-个-binder-服务" class="headerlink" title="2.1 结论：只注册 1 个 binder 服务"></a>2.1 结论：只注册 1 个 binder 服务</h3><p><strong><code>android.hardware.automotive.audiocontrol.IAudioControl/default</code></strong></p><p>VINTF 声明（构建产物 <code>out/.../vendor/etc/vintf/manifest/audiocontrol-default.xml</code>）：<br><code>&lt;name&gt;android.hardware.automotive.audiocontrol&lt;/name&gt;</code> + <code>&lt;version&gt;5&lt;/version&gt;</code> + <code>&lt;fqname&gt;IAudioControl/default&lt;/fqname&gt;</code></p><h3 id="2-2-注册点：default-Service-cpp"><a href="#2-2-注册点：default-Service-cpp" class="headerlink" title="2.2 注册点：default&#x2F;Service.cpp"></a>2.2 注册点：default&#x2F;Service.cpp</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// L29-36 注册 lambda</span></span><br><span class="line"><span class="keyword">auto</span> registerBinderAsService = [](<span class="keyword">auto</span> &amp;&amp;binder, <span class="type">const</span> std::string &amp;serviceName) &#123;</span><br><span class="line">    <span class="type">binder_exception_t</span> status = <span class="built_in">AServiceManager_addService</span>(binder.<span class="built_in">get</span>(), serviceName.<span class="built_in">c_str</span>());</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// L38-51 组装 + 注册</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">registerAudioControl</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    gAudioControl = ndk::SharedRefBase::<span class="built_in">make</span>&lt;AudioControl&gt;();</span><br><span class="line">    std::shared_ptr&lt;GWMAudioControl&gt; pGwmAudioControl = ndk::SharedRefBase::<span class="built_in">make</span>&lt;GWMAudioControl&gt;();</span><br><span class="line">    gAudioControl-&gt;<span class="built_in">setExtension</span>(pGwmAudioControl);            <span class="comment">// 挂载扩展（不注册）</span></span><br><span class="line">    <span class="type">const</span> std::string kServiceName = std::<span class="built_in">string</span>(gAudioControl-&gt;descriptor).<span class="built_in">append</span>(<span class="string">&quot;/default&quot;</span>);</span><br><span class="line">    registerBinderAsService(gAudioControl-&gt;<span class="built_in">asBinder</span>(), kServiceName);   <span class="comment">// ← 唯一的 addService</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// L53-57 导出给 QTI 的入口符号</span></span><br><span class="line"><span class="keyword">extern</span> <span class="string">&quot;C&quot;</span> __attribute__((<span class="built_in">visibility</span>(<span class="string">&quot;default&quot;</span>))) <span class="function"><span class="type">int32_t</span> <span class="title">registerServices</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    registerAudioControl();</span><br><span class="line">    <span class="keyword">return</span> STATUS_OK;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>descriptor 字面量（生成代码 <code>IAudioControl.cpp</code>）：<br><code>const char* IAudioControl::descriptor = &quot;android.hardware.automotive.audiocontrol.IAudioControl&quot;;</code></p><h3 id="2-3-两个易错点"><a href="#2-3-两个易错点" class="headerlink" title="2.3 两个易错点"></a>2.3 两个易错点</h3><ol><li><p><strong>类名 <code>BnAudioControl</code> ≠ 接口名 <code>IAudioControl</code></strong><br>AIDL 生成 Bn&#x2F;Bp 类时会去掉接口名的 <code>I</code> 前缀：<br><code>IAudioControl</code> → <code>BnAudioControl.h</code> &#x2F; <code>BpAudioControl.h</code> + 接口头 <code>IAudioControl.h</code><br>同理 <code>IFocusListener</code> → <code>BnFocusListener.h</code>、<code>IMyService</code> → <code>BnMyService.h</code><br>所以服务名是 <code>.IAudioControl/default</code>，<strong>不是</strong> <code>.AudioControl/default</code></p></li><li><p><strong><code>GWMAudioControl</code> 不是第二个服务</strong><br>它是 <code>BnGWMAudioControl</code> 子类（IGWMAudioControl 接口的实现），但只通过 <code>setExtension()</code> 挂到 AudioControl 上；<br>客户端调的 36 个 <code>gwm_*</code> 方法是直接加在 <code>IAudioControl.aidl</code> 里的，走同一个 binder</p></li></ol><h3 id="2-4-干扰项与死代码识别"><a href="#2-4-干扰项与死代码识别" class="headerlink" title="2.4 干扰项与死代码识别"></a>2.4 干扰项与死代码识别</h3><table><thead><tr><th>对象</th><th>真相</th></tr></thead><tbody><tr><td><code>NoboAudioControl_AHal.cpp</code></td><td><strong>死代码</strong>：未进 Android.mk 源列表、全树 0 引用；内容是反向逻辑（客户端连 <code>android.hardware.audio.core.IModule/default</code>），不是服务端注册</td></tr><tr><td><code>PowerPolicyClient</code></td><td>在 <code>registerAudioControl()</code> 里整段被注释</td></tr><tr><td><code>audiocontrol-default.rc</code></td><td>定义了 init 服务但没有对应二进制被编译，rc 也未进镜像 — 历史遗留</td></tr><tr><td><code>Android.bp_bk</code></td><td>备份文件（文件名不是 Android.bp），不参与构建；其中 <code>init_rc</code> 是注释状态</td></tr></tbody></table><h3 id="2-5-分析方法论（查任意目录注册了什么服务）"><a href="#2-5-分析方法论（查任意目录注册了什么服务）" class="headerlink" title="2.5 分析方法论（查任意目录注册了什么服务）"></a>2.5 分析方法论（查任意目录注册了什么服务）</h3><ol><li><code>grep -rn &quot;AServiceManager_addService\|registerAsService\|addService&quot; &lt;目录&gt;</code> 找注册点</li><li>看 <code>Android.mk</code>&#x2F;<code>Android.bp</code> 源文件列表 → 判断哪些 .cpp 真的被编译（排除死代码）</li><li>追踪导出符号（如 <code>registerServices</code>）被谁 <code>dlsym</code> 调用（查 QTI 的 Service.cpp + 板级 xml）</li><li>用 <code>out/soong</code>（生成物）与 <code>out/target/product</code>（安装物）做最终确认</li></ol><h2 id="三、servicemanager-的-VINTF-声明强制校验"><a href="#三、servicemanager-的-VINTF-声明强制校验" class="headerlink" title="三、servicemanager 的 VINTF 声明强制校验"></a>三、servicemanager 的 VINTF 声明强制校验</h2><h3 id="3-1-机制概述"><a href="#3-1-机制概述" class="headerlink" title="3.1 机制概述"></a>3.1 机制概述</h3><p>标了 <code>@VintfStability</code> 的 AIDL 接口，其服务实例必须事先在设备 VINTF manifest 中声明，否则 servicemanager 会<strong>拒绝注册</strong>。声明缺失时，日志中会出现如下内容（INFO 级别，容易被误认为普通警告）：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Could not find &lt;package&gt;.&lt;interface&gt;/&lt;instance&gt; in the VINTF manifest.</span><br><span class="line">No alternative instances declared in VINTF.</span><br></pre></td></tr></table></figure><h3 id="3-2-校验链路（源码级）"><a href="#3-2-校验链路（源码级）" class="headerlink" title="3.2 校验链路（源码级）"></a>3.2 校验链路（源码级）</h3><p><code>frameworks/native/cmds/servicemanager/ServiceManager.cpp</code> 的 <code>addService()</code> 检查顺序：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// L512-541</span></span><br><span class="line"><span class="number">1.</span> app UID 拒绝（AID_APP 以上不能注册服务）</span><br><span class="line"><span class="number">2.</span> <span class="built_in">canAddService</span>()        ← SELinux 检查（L1130<span class="number">-1134</span>，失败报 <span class="string">&quot;SELinux denied for service.&quot;</span>）</span><br><span class="line"><span class="number">3.</span> <span class="built_in">isValidServiceName</span>()   ← 名字格式（字母/数字/_-./，≤<span class="number">127</span> 字符）</span><br><span class="line"><span class="number">4.</span> <span class="built_in">meetsDeclarationRequirements</span>()  ← VINTF 检查（L536<span class="number">-541</span>）</span><br><span class="line">     <span class="keyword">if</span> (!<span class="built_in">meetsDeclarationRequirements</span>(ctx, binder, name))</span><br><span class="line">         <span class="keyword">return</span> EX_ILLEGAL_ARGUMENT, <span class="string">&quot;VINTF declaration error.&quot;</span>;   ← 注册被拒</span><br></pre></td></tr></table></figure><p><code>meetsDeclarationRequirements</code>（L342-349）：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (!Stability::requiresVintfDeclaration(binder)) <span class="keyword">return</span> <span class="literal">true</span>;   <span class="comment">// 非 @VintfStability 接口跳过</span></span><br><span class="line"><span class="keyword">return</span> <span class="built_in">isVintfDeclared</span>(ctx, name);                               <span class="comment">// 必须能在 VINTF manifest 找到</span></span><br></pre></td></tr></table></figure><ul><li><code>Stability::requiresVintfDeclaration()</code> 实现在 <code>frameworks/native/libs/binder/Stability.cpp</code> L68-70：绑定对象稳定性级别为 VINTF（即接口标了 <code>@VintfStability</code>）时返回 true</li><li><code>isVintfDeclared()</code>（L144-199）：把服务名拆成 <code>package / iface / instance</code>（AidlName），遍历 device + framework 两个 manifest 查 <code>hasAidlInstance()</code></li><li>找不到时打印上述日志并 return false → 注册被拒</li></ul><h3 id="3-3-manifest-来源与缓存机制"><a href="#3-3-manifest-来源与缓存机制" class="headerlink" title="3.3 manifest 来源与缓存机制"></a>3.3 manifest 来源与缓存机制</h3><ul><li>Device manifest &#x3D; <code>/vendor/etc/vintf/manifest.xml</code> + <strong>fragments</strong>（<code>/vendor/etc/vintf/manifest/*.xml</code>），运行时由 libvintf 合并</li><li><code>VintfObject::GetInstance()</code> 是<strong>单例缓存</strong>：servicemanager 启动后第一次读取就缓存，之后不再重读</li><li><strong>改了 fragment 必须重启整机（或 servicemanager）才生效</strong>；仅重启音频 HAL 进程无效</li></ul><h3 id="3-4-报错日志对照表"><a href="#3-4-报错日志对照表" class="headerlink" title="3.4 报错日志对照表"></a>3.4 报错日志对照表</h3><table><thead><tr><th>日志</th><th>含义</th><th>对策</th></tr></thead><tbody><tr><td><code>SELinux: No match for &lt;name&gt; in service_contexts</code></td><td>service_contexts 缺服务名→label 映射</td><td>补 <code>vendor_service_contexts</code></td></tr><tr><td><code>SELinux denied for service.</code></td><td>域对目标 label 无 add 权限</td><td>补 te 规则</td></tr><tr><td><code>Could not find ... in the VINTF manifest</code></td><td>fragment 缺失或更新后未重启</td><td>补 fragment + 重启</td></tr><tr><td><code>Found &lt;name&gt; in device VINTF manifest.</code></td><td>校验通过（成功标志）</td><td>—</td></tr></tbody></table><h3 id="3-5-部署与验证要点"><a href="#3-5-部署与验证要点" class="headerlink" title="3.5 部署与验证要点"></a>3.5 部署与验证要点</h3><ul><li>manifest fragment（如 <code>my-service-manifest.xml</code>）需放到 <code>/vendor/etc/vintf/manifest/</code> 目录下</li><li>更新 fragment 后必须重启整机（或 servicemanager）才生效（VintfObject 启动缓存）</li><li>验证：<code>service list</code> 能查到服务名；servicemanager 日志出现 <code>Found ... in device VINTF manifest.</code></li></ul><h3 id="3-6-fqname-拼装规则"><a href="#3-6-fqname-拼装规则" class="headerlink" title="3.6 fqname 拼装规则"></a>3.6 fqname 拼装规则</h3><p>Manifest fragment 的 <code>&lt;name&gt;</code> + <code>&lt;fqname&gt;</code> 拼接结果必须严格等于注册名：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">&lt;name&gt;vendor.gwm.myservice&lt;/name&gt;  +  &lt;fqname&gt;IMyService/default&lt;/fqname&gt;</span><br><span class="line">   → vendor.gwm.myservice.IMyService/default   ✓ 与 descriptor + &quot;/default&quot; 一致</span><br></pre></td></tr></table></figure><h2 id="四、AIDL-后端：NDK-vs-CPP"><a href="#四、AIDL-后端：NDK-vs-CPP" class="headerlink" title="四、AIDL 后端：NDK vs CPP"></a>四、AIDL 后端：NDK vs CPP</h2><h3 id="4-1-什么是”后端”"><a href="#4-1-什么是”后端”" class="headerlink" title="4.1 什么是”后端”"></a>4.1 什么是”后端”</h3><p>AIDL 是接口定义语言；<code>aidl_interface</code> 模块的 <code>backend &#123;&#125;</code> 块决定从同一份 <code>.aidl</code> 生成<strong>哪种语言&#x2F;运行时</strong>的绑定代码。共 4 种：<strong>ndk、cpp、java、rust</strong>。</p><p><strong>NDK 后端 &#x3D; 生成基于 <code>libbinder_ndk</code> 的 C++ 绑定</strong>（底层是 ABI 稳定的纯 C 接口 <code>AIBinder_*</code>）。注意这里的 “NDK” 不是指 NDK 工具链，而是指绑定跑在 libbinder_ndk 上。HAL &#x2F; vendor 服务（MyService、NoboAudioService、audiocontrol）用的全是它。</p><h3 id="4-2-Soong-默认行为"><a href="#4-2-Soong-默认行为" class="headerlink" title="4.2 Soong 默认行为"></a>4.2 Soong 默认行为</h3><p><code>system/tools/aidl/build/aidl_interface.go</code> L552-581：<strong>四个后端默认全部 enabled&#x3D;true</strong>（注释：故意的，让开发者显式关闭不需要的）。所以：</p><ul><li>AOSP 的 audiocontrol：<code>-V5-cpp</code> &#x2F; <code>-V5-java</code> &#x2F; <code>-V5-ndk</code> 三套都生成</li><li>本工程的 MyService：显式 <code>cpp: &#123; enabled: false &#125;</code> → 生成 <code>-V1-java</code> &#x2F; <code>-V1-ndk</code> &#x2F; <code>-V1-rust</code> 三套</li></ul><h3 id="4-3-对比表"><a href="#4-3-对比表" class="headerlink" title="4.3 对比表"></a>4.3 对比表</h3><table><thead><tr><th>维度</th><th><strong>NDK 后端</strong>（工程在用）</th><th>CPP 后端（libbinder 老式）</th></tr></thead><tbody><tr><td>运行时库</td><td><code>libbinder_ndk</code>（C API + 薄 C++ 封装 <code>ndk::</code>）</td><td><code>libbinder</code>（C++，<code>sp&lt;&gt;</code>&#x2F;<code>IBinder</code>）</td></tr><tr><td>命名空间</td><td><code>aidl::vendor::gwm::myservice</code></td><td><code>android::hardware::...</code>（无 <code>aidl::</code>）</td></tr><tr><td>服务端基类</td><td><code>BnMyService : ::ndk::BnCInterface&lt;IMyService&gt;</code></td><td><code>BnAudioControl : ::android::BnInterface&lt;IAudioControl&gt;</code></td></tr><tr><td>include 路径</td><td><code>&lt;aidl/vendor/gwm/myservice/BnMyService.h&gt;</code>（带 <code>aidl/</code>）</td><td><code>&lt;android/hardware/.../BnFoo.h&gt;</code>（不带）</td></tr><tr><td>返回&#x2F;指针类型</td><td><code>ndk::ScopedAStatus</code>、<code>ndk::SharedRefBase::make&lt;T&gt;()</code>、<code>ndk::SpAIBinder</code></td><td><code>android::binder::Status</code>、<code>sp&lt;&gt;</code>、<code>wp&lt;&gt;</code></td></tr><tr><td>注册 API</td><td><code>AServiceManager_addService</code> &#x2F; <code>_checkService</code></td><td><code>defaultServiceManager()-&gt;addService</code></td></tr><tr><td>Soong 模块名</td><td><code>&lt;name&gt;-V1-ndk</code></td><td><code>&lt;name&gt;-V1-cpp</code></td></tr><tr><td>ABI 稳定性</td><td><strong>纯 C ABI，跨版本稳定</strong></td><td>C++ ABI 不稳定，平台内部使用</td></tr></tbody></table><h3 id="4-4-生成物对比（同一接口两套，基类完全不同）"><a href="#4-4-生成物对比（同一接口两套，基类完全不同）" class="headerlink" title="4.4 生成物对比（同一接口两套，基类完全不同）"></a>4.4 生成物对比（同一接口两套，基类完全不同）</h3><ul><li>CPP 版：<code>out/soong/...-V5-cpp-source/gen/.../BnAudioControl.h</code><br><code>class BnAudioControl : public ::android::BnInterface&lt;IAudioControl&gt;</code></li><li>NDK 版：<code>out/soong/...-V5-ndk-source/gen/include/aidl/.../BnAudioControl.h</code><br><code>class BnAudioControl : public ::ndk::BnCInterface&lt;IAudioControl&gt;</code></li></ul><p>工程代码 include 的是带 <code>aidl/</code> 前缀的版本 → 用的 NDK 后端；链接的也是 <code>android.hardware.automotive.audiocontrol-V5-ndk</code>。</p><h3 id="4-5-为什么-HAL-服务都选-NDK-后端"><a href="#4-5-为什么-HAL-服务都选-NDK-后端" class="headerlink" title="4.5 为什么 HAL 服务都选 NDK 后端"></a>4.5 为什么 HAL 服务都选 NDK 后端</h3><ol><li><strong>ABI 稳定</strong>：只暴露 C ABI，system 升级不需要重编 vendor 进程</li><li><strong>版本化治理</strong>：<code>-V1-ndk</code> 后缀 + <code>update-api</code> &#x2F; <code>freeze-api</code> 冻结流程都围绕它设计</li><li><strong>VINTF 集成</strong>：<code>@VintfStability</code> 标记由 NDK 绑定携带 —— 第三节的强制校验就是针对这类接口</li><li>cpp 后端留给平台内部历史代码；java 面向 framework 应用；rust 是新选项</li></ol><h3 id="4-6-工程实践建议"><a href="#4-6-工程实践建议" class="headerlink" title="4.6 工程实践建议"></a>4.6 工程实践建议</h3><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">backend: &#123;</span><br><span class="line">    cpp:  &#123; enabled: false &#125;,   <span class="comment"># 不需要 libbinder 变体</span></span><br><span class="line">    java: &#123; enabled: false &#125;,   <span class="comment"># 纯 C++ vendor 服务用不上（可选）</span></span><br><span class="line">    rust: &#123; enabled: false &#125;,   <span class="comment"># 同上（可选）</span></span><br><span class="line">    <span class="comment"># ndk 必须保持默认 true，关了就没有 BnMyService 和链接库</span></span><br><span class="line">&#125;,</span><br></pre></td></tr></table></figure><h2 id="五、binder-线程池：程序建池、库复用"><a href="#五、binder-线程池：程序建池、库复用" class="headerlink" title="五、binder 线程池：程序建池、库复用"></a>五、binder 线程池：程序建池、库复用</h2><h3 id="5-1-核心模式"><a href="#5-1-核心模式" class="headerlink" title="5.1 核心模式"></a>5.1 核心模式</h3><p>binder 线程池在一个进程内只需要（也建议只）配置一次：<strong>由主程序在启动时统一建池，各服务库检查后复用；库代码仅在发现进程内没有池时才兜底自建</strong>。这是 <code>libbinder_ndk</code> 头文件中明确的设计约定：</p><blockquote><p>Usually, you should use this in a <strong>library</strong> to abort if the threadpool is not started.<br><strong>Programs should configure binder threadpools once at the beginning.</strong></p></blockquote><h3 id="5-2-时序：QTI-主程序先建池"><a href="#5-2-时序：QTI-主程序先建池" class="headerlink" title="5.2 时序：QTI 主程序先建池"></a>5.2 时序：QTI 主程序先建池</h3><p>QTI <code>main()</code>（<code>primary-hal/hal/service/Service.cpp</code>）：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">ABinderProcess_setThreadPoolMaxThreadCount</span>(<span class="number">16</span>);</span><br><span class="line"><span class="built_in">ABinderProcess_startThreadPool</span>();      <span class="comment">// L117 ← 池在这里就起了</span></span><br><span class="line">registerAvailableInterfaces();         <span class="comment">// L119 ← 之后才 dlopen 各个服务库</span></span><br><span class="line">...</span><br><span class="line"><span class="built_in">ABinderProcess_joinThreadPool</span>();       <span class="comment">// 主线程也加入池</span></span><br></pre></td></tr></table></figure><p>调用链：<code>main()</code> → <code>registerAvailableInterfaces()</code> → dlopen → <code>registerServices()</code> → 各服务注册。<br>因此服务库代码执行到注册逻辑时，进程内已经有 16 个线程的池在运行，服务由主程序的池统一调度。</p><h3 id="5-3-库侧标准写法"><a href="#5-3-库侧标准写法" class="headerlink" title="5.3 库侧标准写法"></a>5.3 库侧标准写法</h3><p>库在注册服务前应检查并复用已有池，没有才自建（兜底）：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (!<span class="built_in">ABinderProcess_isThreadPoolStarted</span>()) &#123;</span><br><span class="line">    <span class="built_in">ALOGI</span>(<span class="string">&quot;No AIDL binder thread pool can be reused, starting new one...&quot;</span>);</span><br><span class="line">    <span class="function">std::thread <span class="title">mainThread</span><span class="params">([]() &#123;</span></span></span><br><span class="line"><span class="params"><span class="function">        ABinderProcess_setThreadPoolMaxThreadCount(<span class="number">14</span>);</span></span></span><br><span class="line"><span class="params"><span class="function">        ABinderProcess_startThreadPool();</span></span></span><br><span class="line"><span class="params"><span class="function">        ABinderProcess_joinThreadPool();</span></span></span><br><span class="line"><span class="params"><span class="function">    &#125;)</span></span>;</span><br><span class="line">    mainThread.<span class="built_in">detach</span>();</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 之后再执行 AServiceManager_addService(...)</span></span><br></pre></td></tr></table></figure><h3 id="5-4-API-语义"><a href="#5-4-API-语义" class="headerlink" title="5.4 API 语义"></a>5.4 API 语义</h3><ul><li><code>ABinderProcess_isThreadPoolStarted</code> 是 Android 14（U，<code>LIBBINDER_NDK34</code>）起公开的 API</li><li>实现（<code>frameworks/native/libs/binder/ndk/process.cpp</code>）：<br><code>return ProcessState::self()-&gt;isThreadPoolStarted();</code></li><li>像 <code>audiohalservice.qti</code> 这类主程序已建池的进程，库内兜底分支不会走到，属正常现象</li></ul><h2 id="六、MyService-交付物与部署清单"><a href="#六、MyService-交付物与部署清单" class="headerlink" title="六、MyService 交付物与部署清单"></a>六、MyService 交付物与部署清单</h2><h3 id="6-1-集成全貌"><a href="#6-1-集成全貌" class="headerlink" title="6.1 集成全貌"></a>6.1 集成全貌</h3><p><strong>命名三要素</strong>：package&#x2F;module <code>vendor.gwm.myservice</code> · 接口 <code>IMyService</code> · 实例 <code>IMyService/default</code></p><p><strong>新增 9 个文件</strong>（<code>vendor/gwm/interfaces/impl/audio/MyService/</code>）：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">aidl/Android.bp                              # aidl_interface 定义</span><br><span class="line">aidl/vendor/gwm/myservice/IMyService.aidl    # 接口（@VintfStability）</span><br><span class="line">impl/Android.mk                              # libMyService.androidmk</span><br><span class="line">impl/include/MyService.h                     # BnMyService 子类</span><br><span class="line">impl/src/MyService.cpp                       # 注册 + 实现</span><br><span class="line">impl/my-service-manifest.xml                 # VINTF fragment</span><br><span class="line">impl/my-service-compatibility.xml            # FCM（framework 兼容矩阵）</span><br><span class="line">impl/sepolicy/service_contexts               # 服务名 → hal_audio_service</span><br><span class="line">impl/sepolicy/my-service.te                  # binder_call 规则</span><br></pre></td></tr></table></figure><p><strong>修改 4 个文件</strong>：<code>ModulePrimary.h/.cpp</code>（注册入口）、<code>hal/core/Android.mk</code>（链接新库）、<code>audio_install.mk</code>（PRODUCT_PACKAGES &#x2F; sepolicy &#x2F; FCM 接线）</p><h3 id="6-2-设备端部署清单（手动迭代时）"><a href="#6-2-设备端部署清单（手动迭代时）" class="headerlink" title="6.2 设备端部署清单（手动迭代时）"></a>6.2 设备端部署清单（手动迭代时）</h3><p><strong>核心库（3 个，缺一不可）</strong>：</p><table><thead><tr><th>构建产物</th><th>车机路径</th><th>说明</th></tr></thead><tbody><tr><td><code>vendor/lib64/libMyService.androidmk.so</code></td><td><code>/vendor/lib64/</code></td><td>服务实现库</td></tr><tr><td><code>vendor/lib64/vendor.gwm.myservice-V1-ndk.so</code></td><td><code>/vendor/lib64/</code></td><td>AIDL 生成库（NDK 后端）</td></tr><tr><td><code>vendor/lib64/hw/libaudiocorehal.qti.so</code></td><td><code>/vendor/lib64/hw/</code></td><td>注册入口所在（更新）；缺了会 dlopen 失败 → 整车无音频</td></tr></tbody></table><p><strong>清单与策略</strong>：</p><table><thead><tr><th>文件</th><th>车机路径</th><th>作用</th></tr></thead><tbody><tr><td><code>my-service-manifest.xml</code></td><td><code>/vendor/etc/vintf/manifest/</code></td><td>VINTF 声明（漏推 → 第三节的拒绝注册）</td></tr><tr><td><code>vendor_service_contexts</code></td><td><code>/vendor/etc/selinux/</code></td><td>服务名 → label（复用 <code>hal_audio_service</code>）</td></tr><tr><td><code>precompiled_sepolicy</code></td><td><code>/vendor/etc/selinux/</code></td><td>含 my-service.te 规则</td></tr><tr><td>FCM（<code>compatibility_matrix.device.xml</code>）</td><td><code>/system/etc/vintf/</code></td><td>仅当客户端在 system&#x2F;framework 侧时需要</td></tr></tbody></table><h3 id="6-3-构建命令"><a href="#6-3-构建命令" class="headerlink" title="6.3 构建命令"></a>6.3 构建命令</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 首次修改 .aidl 后必须先跑（生成 current/ 快照）</span></span><br><span class="line">m vendor.gwm.myservice-update-api</span><br><span class="line"></span><br><span class="line"><span class="comment"># 增量验证</span></span><br><span class="line">m libMyService.androidmk libaudiocorehal.qti</span><br><span class="line"></span><br><span class="line"><span class="comment"># 上机定版前（冻结为 V1，之后模块名保持不变）</span></span><br><span class="line">m vendor.gwm.myservice-freeze-api</span><br></pre></td></tr></table></figure><h3 id="6-4-命名规律速记"><a href="#6-4-命名规律速记" class="headerlink" title="6.4 命名规律速记"></a>6.4 命名规律速记</h3><ul><li>实现库：<code>libMyService.androidmk.so</code>（本工程惯例，Android.mk 构建）</li><li>AIDL 库：<code>vendor.gwm.myservice-V1-ndk.so</code>（<code>-V&lt;版本&gt;-&lt;后端&gt;</code> 后缀）</li><li>服务名：<code>&lt;package&gt;.&lt;接口&gt;/&lt;实例&gt;</code> &#x3D; <code>vendor.gwm.myservice.IMyService/default</code></li></ul><h2 id="附录：关键源码索引"><a href="#附录：关键源码索引" class="headerlink" title="附录：关键源码索引"></a>附录：关键源码索引</h2><table><thead><tr><th>文件</th><th>作用</th></tr></thead><tbody><tr><td><code>frameworks/native/cmds/servicemanager/ServiceManager.cpp</code></td><td>addService 校验链（SELinux → VINTF → 注册）</td></tr><tr><td><code>frameworks/native/cmds/servicemanager/Access.cpp</code></td><td>service_contexts 标签查找与 SELinux 判定</td></tr><tr><td><code>frameworks/native/libs/binder/Stability.cpp</code></td><td><code>requiresVintfDeclaration</code>（@VintfStability 判定）</td></tr><tr><td><code>frameworks/native/libs/binder/ndk/process.cpp</code></td><td><code>ABinderProcess_*</code>（线程池 API 实现）</td></tr><tr><td><code>frameworks/native/libs/binder/ndk/include_platform/android/binder_process.h</code></td><td>线程池 API 设计意图（程序建池、库复用）</td></tr><tr><td><code>system/tools/aidl/build/aidl_interface.go</code></td><td>各后端默认值（L552-581 默认全开）</td></tr><tr><td><code>vendor/qcom/opensource/audio-hal-ar/primary-hal/hal/service/Service.cpp</code></td><td>QTI AHAL 主程序：建池 → dlopen 注册 → join</td></tr><tr><td><code>vendor/qcom/opensource/audio-hal-ar/primary-hal/hal/service/ConfigManager.cpp</code></td><td>板级 <code>vendor_audio_interfaces.xml</code> 解析</td></tr><tr><td><code>vendor/gwm/interfaces/impl/audio/AutomotiveAudioControl/default/Service.cpp</code></td><td><code>registerServices</code> &#x2F; 单服务注册范例</td></tr><tr><td><code>vendor/gwm/interfaces/impl/audio/MyService/impl/src/MyService.cpp</code></td><td>线程池复用 + addService 范式</td></tr></tbody></table>]]></content>
      
      
      <categories>
          
          <category> Android </category>
          
      </categories>
      
      
        <tags>
            
            <tag> AIDL </tag>
            
            <tag> Audio HAL </tag>
            
            <tag> Binder </tag>
            
            <tag> VINTF </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>理解aidl 在audio FW/HAL中的应用</title>
      <link href="/2026/09/10/%E7%90%86%E8%A7%A3aidl-%E5%9C%A8audio-FW-HAL%E4%B8%AD%E7%9A%84%E5%BA%94%E7%94%A8/"/>
      <url>/2026/09/10/%E7%90%86%E8%A7%A3aidl-%E5%9C%A8audio-FW-HAL%E4%B8%AD%E7%9A%84%E5%BA%94%E7%94%A8/</url>
      
        <content type="html"><![CDATA[<p>本文记录 sa8797 平台上 Audio FW 与 Audio HAL 进行进程间通信的 AIDL 接口，并讲解其中比较重要的几个接口。</p><h2 id="一、背景：从-HIDL-到-AIDL-FMQ"><a href="#一、背景：从-HIDL-到-AIDL-FMQ" class="headerlink" title="一、背景：从 HIDL 到 AIDL + FMQ"></a>一、背景：从 HIDL 到 AIDL + FMQ</h2><p>sa8797 项目使用的是 Android 16，与 Android 14 以前不一样：FW 与 HAL 交互使用的是 <strong>AIDL + FMQ</strong>，去掉了 HIDL。但本质上 HIDL&#x2F;AIDL 底层都是使用 Binder 进行通信。</p><p>Binder 通信的几个基础点：</p><ul><li>内核 Binder 驱动通过 mmap 映射了 <strong>128KB</strong> 的接收缓冲区，数据直接拷贝到目标进程的 mmap 区域，无需中间再拷贝到内核、再拷贝出来。</li><li>Binder 驱动维护线程池，默认 <strong>16 个线程</strong>，请求并发处理，因此 <strong>Server 端方法需保证线程安全</strong>。</li><li>HIDL 中常提到的 <strong>passthrough 模式</strong>：HAL 实现库是以直接 dlopen 方式加载（in-process）；而 <strong>binderized 模式</strong>：HAL 是独立进程，通过 Binder 通信。</li></ul><h2 id="二、核心接口：IModule-aidl"><a href="#二、核心接口：IModule-aidl" class="headerlink" title="二、核心接口：IModule.aidl"></a>二、核心接口：IModule.aidl</h2><p>文件路径：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Y:\code\v6\android\hardware\interfaces\audio\aidl\android\hardware\audio\core\IModule.aidl</span><br></pre></td></tr></table></figure><p>这个 AIDL 是 FW 与 HAL 交互的重要接口定义，替代了 HIDL 的 <code>IDevicesFactory</code> + <code>IDevice</code>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">interface</span> <span class="title class_">IModule</span> &#123;</span><br><span class="line">    …….</span><br><span class="line">    <span class="meta">@VintfStability</span></span><br><span class="line">    parcelable OpenOutputStreamArguments &#123;</span><br><span class="line">        <span class="comment">/** The ID of the audio mix port config. */</span></span><br><span class="line">        <span class="type">int</span> portConfigId;</span><br><span class="line">        <span class="comment">/** Description of the audio that will be played. */</span></span><br><span class="line">        SourceMetadata sourceMetadata;</span><br><span class="line">        <span class="comment">/** Additional information used for offloaded playback only. */</span></span><br><span class="line">        <span class="meta">@nullable</span> AudioOffloadInfo offloadInfo;</span><br><span class="line">        <span class="comment">/** Requested audio I/O buffer minimum size, in frames. */</span></span><br><span class="line">        <span class="type">long</span> bufferSizeFrames;</span><br><span class="line">        <span class="comment">/** Client callback interface for the non-blocking output mode. */</span></span><br><span class="line">        <span class="meta">@nullable</span> IStreamCallback callback;</span><br><span class="line">        <span class="comment">/** Optional callback to notify client about stream events. */</span></span><br><span class="line">        <span class="meta">@nullable</span> IStreamOutEventCallback eventCallback;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@VintfStability</span></span><br><span class="line">    parcelable OpenOutputStreamReturn &#123;</span><br><span class="line">        IStreamOut stream;</span><br><span class="line">        StreamDescriptor desc;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="type">OpenOutputStreamReturn</span> <span class="title function_">openOutputStream</span><span class="params">(in OpenOutputStreamArguments args)</span>;</span><br><span class="line">    ……</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>parcelable</code> 可以理解成一个结构体定义。其中的 <code>IStreamOut</code>、<code>StreamDescriptor</code> 都是利用 AIDL 定义的 parcelable，<code>StreamDescriptor.aidl</code> 就是定义该结构体的文件，感兴趣可以去里面看一下具体有哪些结构的定义。</p><p><code>openOutputStream</code> 返回打开的 stream 流时，会创建 FMQ 的描述：上层通过 <code>OpenOutputStreamReturn</code> 的返回值拿到底层 HAL 创建的 FMQ 描述符，进而与 Audio HAL 进行通信。</p><p><strong>为什么 Android 要定义 AIDL 文件来当结构体定义，而不直接通过 <code>.h</code> 文件定义？</strong></p><p>因为 <code>StreamDescriptor</code> 定义的内容会被 HAL 层、FW、甚至 App 侧引用；使用 AIDL 生成的头文件，就不需要维护三套定义。</p><h3 id="2-1-openOutputStream-的入参"><a href="#2-1-openOutputStream-的入参" class="headerlink" title="2.1 openOutputStream 的入参"></a>2.1 openOutputStream 的入参</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">parcelable OpenOutputStreamArguments &#123;</span><br><span class="line">    <span class="type">int</span> portConfigId;                            <span class="comment">// setAudioPortConfig 拿到的混音端口配置 ID</span></span><br><span class="line">    SourceMetadata sourceMetadata;               <span class="comment">// 播放内容描述</span></span><br><span class="line">    <span class="meta">@nullable</span> AudioOffloadInfo offloadInfo;      <span class="comment">// 仅 offload 播放时需要</span></span><br><span class="line">    <span class="type">long</span> bufferSizeFrames;                       <span class="comment">// 请求的缓冲区最小帧数</span></span><br><span class="line">    <span class="meta">@nullable</span> IStreamCallback callback;          <span class="comment">// 非阻塞模式的客户端回调</span></span><br><span class="line">    <span class="meta">@nullable</span> IStreamOutEventCallback eventCallback;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>AParcel_readData</code> 遇到 <code>IStreamCallback</code> 这种 binder 字段时，会把 parcel 里的强 binder 就地构建成 <code>BpStreamCallback</code> 代理（服务端于是在 HAL 进程持有了指向 fw 回调的代理）。</p><h3 id="2-2-返回值-OpenOutputStreamReturn"><a href="#2-2-返回值-OpenOutputStreamReturn" class="headerlink" title="2.2 返回值 OpenOutputStreamReturn"></a>2.2 返回值 OpenOutputStreamReturn</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">parcelable OpenOutputStreamReturn &#123;</span><br><span class="line">    IStreamOut stream;        <span class="comment">// 新 stream 的强 binder（嵌套 binder 写进 parcel）</span></span><br><span class="line">    StreamDescriptor desc;    <span class="comment">// 含 MQDescriptor：command/reply/audio 三组 FMQ</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>写回时的两个关键动作：</p><ul><li><code>stream</code> 作为<strong>嵌套 binder</strong> 序列化：fw 拿到一个新的 <code>BpStreamOut</code> 代理，之后所有流级控制都走它；</li><li><code>desc</code> 里的 <code>MQDescriptor&lt;...&gt;</code> 序列化会携带 <strong>file descriptor</strong>：Binder 驱动把 fd 转发到 fw 进程，双方 mmap 同一块 FMQ 共享内存——这就是 <code>openOutputStream</code> 之后数据面<strong>零拷贝</strong>的由来。</li></ul><h2 id="三、AIDL-的编译配置：Android-bp"><a href="#三、AIDL-的编译配置：Android-bp" class="headerlink" title="三、AIDL 的编译配置：Android.bp"></a>三、AIDL 的编译配置：Android.bp</h2><p>写好的 AIDL 会通过 <code>Android.bp</code> 进行编译：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br></pre></td><td class="code"><pre><span class="line">aidl_interface &#123;</span><br><span class="line">    name: <span class="string">&quot;android.hardware.audio.core&quot;</span>,</span><br><span class="line">    defaults: [</span><br><span class="line">        <span class="string">&quot;android.hardware.audio_defaults&quot;</span>,</span><br><span class="line">        <span class="string">&quot;latest_android_hardware_audio_common_import_interface&quot;</span>,</span><br><span class="line">        <span class="string">&quot;latest_android_hardware_audio_core_sounddose_import_interface&quot;</span>,</span><br><span class="line">        <span class="string">&quot;latest_android_hardware_audio_effect_import_interface&quot;</span>,</span><br><span class="line">        <span class="string">&quot;latest_android_media_audio_common_types_import_interface&quot;</span>,</span><br><span class="line">    ],</span><br><span class="line">    srcs: [</span><br><span class="line">        <span class="string">&quot;android/hardware/audio/core/AudioPatch.aidl&quot;</span>,</span><br><span class="line">        <span class="string">&quot;android/hardware/audio/core/AudioRoute.aidl&quot;</span>,</span><br><span class="line">        <span class="string">&quot;android/hardware/audio/core/IBluetooth.aidl&quot;</span>,</span><br><span class="line">        <span class="string">&quot;android/hardware/audio/core/IBluetoothA2dp.aidl&quot;</span>,</span><br><span class="line">        <span class="string">&quot;android/hardware/audio/core/IBluetoothLe.aidl&quot;</span>,</span><br><span class="line">        <span class="string">&quot;android/hardware/audio/core/IConfig.aidl&quot;</span>,</span><br><span class="line">        <span class="string">&quot;android/hardware/audio/core/IModule.aidl&quot;</span>,</span><br><span class="line">        <span class="string">&quot;android/hardware/audio/core/IStreamCallback.aidl&quot;</span>,</span><br><span class="line">        <span class="string">&quot;android/hardware/audio/core/IStreamCommon.aidl&quot;</span>,</span><br><span class="line">        <span class="string">&quot;android/hardware/audio/core/IStreamIn.aidl&quot;</span>,</span><br><span class="line">        <span class="string">&quot;android/hardware/audio/core/IStreamOut.aidl&quot;</span>,</span><br><span class="line">        <span class="string">&quot;android/hardware/audio/core/IStreamOutEventCallback.aidl&quot;</span>,</span><br><span class="line">        <span class="string">&quot;android/hardware/audio/core/ITelephony.aidl&quot;</span>,</span><br><span class="line">        <span class="string">&quot;android/hardware/audio/core/MmapBufferDescriptor.aidl&quot;</span>,</span><br><span class="line">        <span class="string">&quot;android/hardware/audio/core/ModuleDebug.aidl&quot;</span>,</span><br><span class="line">        <span class="string">&quot;android/hardware/audio/core/StreamDescriptor.aidl&quot;</span>,</span><br><span class="line">        <span class="string">&quot;android/hardware/audio/core/SurroundSoundConfig.aidl&quot;</span>,</span><br><span class="line">        <span class="string">&quot;android/hardware/audio/core/VendorParameter.aidl&quot;</span>,</span><br><span class="line">    ],</span><br><span class="line">    imports: [</span><br><span class="line">        <span class="string">&quot;android.hardware.common-V2&quot;</span>,</span><br><span class="line">        <span class="string">&quot;android.hardware.common.fmq-V1&quot;</span>,</span><br><span class="line">    ],</span><br><span class="line">    backend: &#123;</span><br><span class="line">        // The C++ backend <span class="keyword">is</span> disabled transitively due to use of FMQ.</span><br><span class="line">        cpp: &#123;</span><br><span class="line">            enabled: false,</span><br><span class="line">        &#125;,</span><br><span class="line">        java: &#123;</span><br><span class="line">            sdk_version: <span class="string">&quot;module_current&quot;</span>,</span><br><span class="line">        &#125;,</span><br><span class="line">    &#125;,</span><br><span class="line">    versions_with_info: [</span><br><span class="line">        &#123;</span><br><span class="line">            version: <span class="string">&quot;1&quot;</span>,</span><br><span class="line">            imports: [</span><br><span class="line">                <span class="string">&quot;android.hardware.common-V2&quot;</span>,</span><br><span class="line">                <span class="string">&quot;android.hardware.common.fmq-V1&quot;</span>,</span><br><span class="line">                <span class="string">&quot;android.hardware.audio.common-V2&quot;</span>,</span><br><span class="line">                <span class="string">&quot;android.hardware.audio.core.sounddose-V1&quot;</span>,</span><br><span class="line">                <span class="string">&quot;android.hardware.audio.effect-V1&quot;</span>,</span><br><span class="line">                <span class="string">&quot;android.media.audio.common.types-V2&quot;</span>,</span><br><span class="line">            ],</span><br><span class="line">        &#125;,</span><br><span class="line">        &#123;</span><br><span class="line">            version: <span class="string">&quot;2&quot;</span>,</span><br><span class="line">            imports: [</span><br><span class="line">                <span class="string">&quot;android.media.audio.common.types-V3&quot;</span>,</span><br><span class="line">                <span class="string">&quot;android.hardware.audio.effect-V2&quot;</span>,</span><br><span class="line">                <span class="string">&quot;android.hardware.audio.core.sounddose-V2&quot;</span>,</span><br><span class="line">                <span class="string">&quot;android.hardware.audio.common-V3&quot;</span>,</span><br><span class="line">                <span class="string">&quot;android.hardware.common-V2&quot;</span>,</span><br><span class="line">                <span class="string">&quot;android.hardware.common.fmq-V1&quot;</span>,</span><br><span class="line">            ],</span><br><span class="line">        &#125;,</span><br><span class="line">        &#123;</span><br><span class="line">            version: <span class="string">&quot;3&quot;</span>,</span><br><span class="line">            imports: [</span><br><span class="line">                <span class="string">&quot;android.media.audio.common.types-V4&quot;</span>,</span><br><span class="line">                <span class="string">&quot;android.hardware.audio.effect-V3&quot;</span>,</span><br><span class="line">                <span class="string">&quot;android.hardware.audio.core.sounddose-V3&quot;</span>,</span><br><span class="line">                <span class="string">&quot;android.hardware.audio.common-V4&quot;</span>,</span><br><span class="line">                <span class="string">&quot;android.hardware.common-V2&quot;</span>,</span><br><span class="line">                <span class="string">&quot;android.hardware.common.fmq-V1&quot;</span>,</span><br><span class="line">            ],</span><br><span class="line">        &#125;,</span><br><span class="line"></span><br><span class="line">        // IMPORTANT: Update latest_android_hardware_audio_core every time you</span><br><span class="line">        // add the latest frozen version to versions_with_info</span><br><span class="line">    ],</span><br><span class="line">    frozen: false,</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// Note: This should always be one version ahead of the last frozen version</span><br><span class="line">latest_android_hardware_audio_core = <span class="string">&quot;android.hardware.audio.core-V4&quot;</span></span><br></pre></td></tr></table></figure><p><code>Android.bp</code> 中 <code>aidl_interface &#123; name: &quot;android.hardware.audio.core&quot; &#125;</code> 关键点：</p><ul><li><code>stability: &quot;vintf&quot;</code>：稳定 AIDL，必须冻结快照 + VINTF 校验；</li><li><code>versions_with_info</code>：V1&#x2F;V2&#x2F;V3 已冻结；<code>frozen: false</code> → 当前 V4 还在开发（<code>latest_android_hardware_audio_core = &quot;android.hardware.audio.core-V4&quot;</code>）；</li><li><code>backend</code>：<code>cpp: enabled: false</code>（注释写明 “disabled transitively due to use of FMQ”，即 <code>StreamDescriptor</code> 里的 <code>MQDescriptor</code> 只能用 NDK 版 <code>libbinder_ndk</code> 编）；java 开启；ndk&#x2F;rust 默认开启；</li><li><code>aidl_api/android.hardware.audio.core/&#123;1,2,3,current&#125;/</code> 是冻结 API 快照，编译时 aidl 工具用 <code>--previous_api_dir=…/3 --previous_hash eb5f1f5a…</code> 做兼容性检查（改接口必须同步 <code>update-api</code>）。</li></ul><h2 id="四、AIDL-生成物与关键代码分析"><a href="#四、AIDL-生成物与关键代码分析" class="headerlink" title="四、AIDL 生成物与关键代码分析"></a>四、AIDL 生成物与关键代码分析</h2><p>AIDL 生成命令会生成对应的 <code>*.h</code> &#x2F; <code>*.cpp</code> 文件，供 FW 以及 HAL 引用，我的项目上生成的地址在：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">out\soong\.intermediates\hardware\interfaces\audio\aidl\android.hardware.audio.core-V4-ndk-source\gen\android\hardware\audio\core\IModule.cpp</span><br><span class="line">out\soong\.intermediates\hardware\interfaces\audio\aidl\android.hardware.audio.core-V4-ndk-source\gen\include\aidl\android\hardware\audio\core\IModule.h</span><br></pre></td></tr></table></figure><ul><li><code>IModule.cpp</code>：<strong>非常重要</strong>，负责 Binder 编解码实现（<code>onTransact</code> 分发 + Bp 各方法序列化）；</li><li><code>IModule.h</code>：接口类本体（fw&#x2F;HAL 都 include 它）；</li><li><code>BnModule.h</code>：服务端基类 <code>BnModule : ndk::BnCInterface&lt;IModule&gt;</code>，HAL 继承它，含 <code>IModuleDelegator</code> 版本委托；</li><li><code>BpModule.h</code>：客户端代理 <code>BpModule : ndk::BpCInterface&lt;IModule&gt;</code>，缓存 version&#x2F;hash。</li></ul><h3 id="4-1-IModule-h-——-接口类本体"><a href="#4-1-IModule-h-——-接口类本体" class="headerlink" title="4.1 IModule.h —— 接口类本体"></a>4.1 IModule.h —— 接口类本体</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// IModule.h</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">IModule</span> : <span class="keyword">public</span> ::ndk::ICInterface &#123;</span><br><span class="line">  <span class="type">static</span> <span class="type">const</span> <span class="type">char</span>* descriptor;                 <span class="comment">// &quot;android.hardware.audio.core.IModule&quot;</span></span><br><span class="line">  <span class="function"><span class="type">static</span> std::shared_ptr&lt;IModule&gt; <span class="title">fromBinder</span><span class="params">(<span class="type">const</span> ::ndk::SpAIBinder&amp; binder)</span></span>;</span><br><span class="line">  <span class="type">static</span> <span class="keyword">inline</span> <span class="type">const</span> <span class="type">int32_t</span> version = <span class="literal">true</span> ? <span class="number">3</span> : <span class="number">4</span>;    <span class="comment">// ← 版本降级，见下文说明</span></span><br><span class="line">  <span class="type">static</span> <span class="keyword">inline</span> <span class="type">const</span> std::string hash  = <span class="literal">true</span> ? <span class="string">&quot;eb5f1f5af402830af56b50eb66bdd60fd8cc774e&quot;</span> : <span class="string">&quot;notfrozen&quot;</span>;</span><br><span class="line">  <span class="type">static</span> <span class="keyword">constexpr</span> <span class="type">uint32_t</span> TRANSACTION_openOutputStream = FIRST_CALL_TRANSACTION + <span class="number">14</span>;</span><br><span class="line">  ...</span><br><span class="line">  <span class="keyword">virtual</span> ::<span class="function">ndk::ScopedAStatus <span class="title">openOutputStream</span><span class="params">(<span class="type">const</span> OpenOutputStreamArguments&amp; in_args,</span></span></span><br><span class="line"><span class="params"><span class="function">                                                OpenOutputStreamReturn* _aidl_return)</span> </span>= <span class="number">0</span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">IModuleDefault</span> : <span class="keyword">public</span> IModule &#123;  <span class="comment">// 全部方法返回 EX_UNSUPPORTED_OPERATION 的兜底实现</span></span><br><span class="line">  ...</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>关于版本降级：V4 目前处于开发中（<code>frozen: false</code>），在 Release 配置（<code>RELEASE_AIDL_USE_UNFROZEN=false</code>）下，生成代码对外仍按已冻结的 V3 协商版本与哈希（hash 取 V3 的 <code>eb5f1f5af402830af56b50eb66bdd60fd8cc774e</code>）；若为 unfrozen 开发模式，则对外声明 V4（hash 为 <code>notfrozen</code>）。</p><h3 id="4-2-BnModule-h-——-HAL-要继承的基类"><a href="#4-2-BnModule-h-——-HAL-要继承的基类" class="headerlink" title="4.2 BnModule.h —— HAL 要继承的基类"></a>4.2 BnModule.h —— HAL 要继承的基类</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// BnModule.h —— HAL 要继承的基类</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">BnModule</span> : <span class="keyword">public</span> ::ndk::BnCInterface&lt;IModule&gt; &#123;</span><br><span class="line">  ::<span class="function">ndk::ScopedAStatus <span class="title">getInterfaceVersion</span><span class="params">(<span class="type">int32_t</span>*)</span> <span class="keyword">final</span></span>;   <span class="comment">// 版本协商走这里</span></span><br><span class="line">  ::<span class="function">ndk::ScopedAStatus <span class="title">getInterfaceHash</span><span class="params">(std::string*)</span> <span class="keyword">final</span></span>;</span><br><span class="line">  ::<span class="function">ndk::SpAIBinder <span class="title">createBinder</span><span class="params">()</span> <span class="keyword">override</span></span>;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">IModuleDelegator</span> : <span class="keyword">public</span> BnModule &#123; ... &#125;;  <span class="comment">// 代理解析，校验 version 一致性</span></span><br></pre></td></tr></table></figure><h3 id="4-3-BpModule-h-——-fw-侧代理"><a href="#4-3-BpModule-h-——-fw-侧代理" class="headerlink" title="4.3 BpModule.h —— fw 侧代理"></a>4.3 BpModule.h —— fw 侧代理</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// BpModule.h —— fw 侧代理</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">BpModule</span> : <span class="keyword">public</span> ::ndk::BpCInterface&lt;IModule&gt; &#123;</span><br><span class="line">  ::<span class="function">ndk::ScopedAStatus <span class="title">openOutputStream</span><span class="params">(<span class="type">const</span> OpenOutputStreamArguments&amp;, OpenOutputStreamReturn*)</span> <span class="keyword">override</span></span>;</span><br><span class="line">  ...</span><br><span class="line">  <span class="type">int32_t</span> _aidl_cached_version = <span class="number">-1</span>;      <span class="comment">// 首次 getInterfaceVersion 后缓存</span></span><br><span class="line">  std::string _aidl_cached_hash = <span class="string">&quot;-1&quot;</span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><h3 id="4-4-IModule-cpp-——-onTransact-服务端总入口"><a href="#4-4-IModule-cpp-——-onTransact-服务端总入口" class="headerlink" title="4.4 IModule.cpp —— onTransact 服务端总入口"></a>4.4 IModule.cpp —— onTransact 服务端总入口</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">static</span> <span class="type">binder_status_t</span> _aidl_android_hardware_audio_core_IModule_onTransact(AIBinder* _aidl_binder, <span class="type">transaction_code_t</span> _aidl_code, <span class="type">const</span> AParcel* _aidl_in, AParcel* _aidl_out) &#123;</span><br><span class="line">  (<span class="type">void</span>)_aidl_in;</span><br><span class="line">  (<span class="type">void</span>)_aidl_out;</span><br><span class="line">  <span class="type">binder_status_t</span> _aidl_ret_status = STATUS_UNKNOWN_TRANSACTION;</span><br><span class="line">  std::shared_ptr&lt;BnModule&gt; _aidl_impl = std::<span class="built_in">static_pointer_cast</span>&lt;BnModule&gt;(::ndk::ICInterface::<span class="built_in">asInterface</span>(_aidl_binder));</span><br><span class="line">  <span class="keyword">switch</span> (_aidl_code) &#123;</span><br><span class="line">    …..</span><br><span class="line">    <span class="keyword">case</span> (FIRST_CALL_TRANSACTION + <span class="number">14</span> <span class="comment">/*openOutputStream*/</span>): &#123;</span><br><span class="line">          ::aidl::android::hardware::audio::core::IModule::OpenOutputStreamArguments in_args;</span><br><span class="line">          ::aidl::android::hardware::audio::core::IModule::OpenOutputStreamReturn _aidl_return;</span><br><span class="line">          _aidl_ret_status = ::ndk::<span class="built_in">AParcel_readData</span>(_aidl_in, &amp;in_args);</span><br><span class="line">          <span class="keyword">if</span> (_aidl_ret_status != STATUS_OK) <span class="keyword">break</span>;</span><br><span class="line">          ::ndk::ScopedAStatus _aidl_status = _aidl_impl-&gt;<span class="built_in">openOutputStream</span>(in_args, &amp;_aidl_return);</span><br><span class="line">          _aidl_ret_status = <span class="built_in">AParcel_writeStatusHeader</span>(_aidl_out, _aidl_status.<span class="built_in">get</span>());</span><br><span class="line">          <span class="keyword">if</span> (_aidl_ret_status != STATUS_OK) <span class="keyword">break</span>;</span><br><span class="line">          <span class="keyword">if</span> (!<span class="built_in">AStatus_isOk</span>(_aidl_status.<span class="built_in">get</span>())) <span class="keyword">break</span>;</span><br><span class="line">          _aidl_ret_status = ::ndk::<span class="built_in">AParcel_writeData</span>(_aidl_out, _aidl_return);</span><br><span class="line">          <span class="keyword">if</span> (_aidl_ret_status != STATUS_OK) <span class="keyword">break</span>;</span><br><span class="line">          <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    ……..</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>它是 NDK 后端 AIDL 生成的<strong>“服务端总入口”</strong>：每个 Binder 线程收到该接口的事务后，都会先落到这个静态函数。</p><p><strong>（1）<code>_aidl_binder</code> → C++ 对象</strong></p><p><code>ICInterface::asInterface()</code> 把裸 <code>AIBinder*</code> 还原成进程内的 <code>ICInterface</code>，再 <code>static_pointer_cast&lt;BnModule&gt;</code> 变成服务对象。之后每个 case 里的 <code>_aidl_impl-&gt;openOutputStream(...)</code> 都是虚函数调用——所以它最终调到 QTI 的 <code>ModulePrimary</code>（继承链 <code>ModulePrimary : Module : BnModule</code>）。<strong>生成代码永远不认识你的类，只认识 <code>BnModule</code> 基类。</strong></p><p><strong>（2）事务名与 class 定义</strong></p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">static</span> <span class="type">const</span> <span class="type">char</span>* _g_aidl_android_hardware_audio_core_IModule_clazz_names[] = &#123;</span><br><span class="line">  <span class="string">&quot;setModuleDebug&quot;</span>, <span class="string">&quot;getTelephony&quot;</span>, ..., <span class="string">&quot;openOutputStream&quot;</span>, ... <span class="string">&quot;prepareToDisconnectExternalDevice&quot;</span>,</span><br><span class="line">&#125;;   <span class="comment">// 41 个事务名，仅用于调试/错误日志</span></span><br><span class="line"></span><br><span class="line"><span class="type">static</span> AIBinder_Class* _g_aidl_android_hardware_audio_core_IModule_clazz =</span><br><span class="line">    ::ndk::ICInterface::<span class="built_in">defineClass</span>(IModule::descriptor,</span><br><span class="line">                                    _aidl_android_hardware_audio_core_IModule_onTransact,</span><br><span class="line">                                    _g_aidl_..._names, <span class="number">41</span>);</span><br></pre></td></tr></table></figure><p><strong>（3）createBinder()</strong></p><p><code>BnModule::createBinder()</code>（HAL 进程 <code>AServiceManager_addService</code> 注册时调用）负责把对象和这个 class 绑成 binder：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">::<span class="function">ndk::SpAIBinder <span class="title">BnModule::createBinder</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  AIBinder* binder = <span class="built_in">AIBinder_new</span>(_g_aidl_..._clazz, <span class="keyword">static_cast</span>&lt;<span class="type">void</span>*&gt;(<span class="keyword">this</span>));</span><br><span class="line">  <span class="meta">#<span class="keyword">ifdef</span> BINDER_STABILITY_SUPPORT</span></span><br><span class="line">  <span class="built_in">AIBinder_markVintfStability</span>(binder);</span><br><span class="line">  <span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line">  <span class="keyword">return</span> ::ndk::<span class="built_in">SpAIBinder</span>(binder);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="4-5-openOutputStream-事务的处理模板"><a href="#4-5-openOutputStream-事务的处理模板" class="headerlink" title="4.5 openOutputStream 事务的处理模板"></a>4.5 openOutputStream 事务的处理模板</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">case</span> (FIRST_CALL_TRANSACTION + <span class="number">14</span> <span class="comment">/*openOutputStream*/</span>): &#123;</span><br><span class="line">  ::...::IModule::OpenOutputStreamArguments in_args;      <span class="comment">// 入参 parcelable</span></span><br><span class="line">  ::...::IModule::OpenOutputStreamReturn _aidl_return;    <span class="comment">// 返回值 parcelable</span></span><br><span class="line"></span><br><span class="line">  _aidl_ret_status = ::ndk::<span class="built_in">AParcel_readData</span>(_aidl_in, &amp;in_args);   <span class="comment">// ① 反序列化入参</span></span><br><span class="line">  <span class="keyword">if</span> (_aidl_ret_status != STATUS_OK) <span class="keyword">break</span>;</span><br><span class="line"></span><br><span class="line">  ::ndk::ScopedAStatus _aidl_status = _aidl_impl-&gt;<span class="built_in">openOutputStream</span>(in_args, &amp;_aidl_return);  <span class="comment">// ② 虚调用到 ModulePrimary</span></span><br><span class="line">  _aidl_ret_status = <span class="built_in">AParcel_writeStatusHeader</span>(_aidl_out, _aidl_status.<span class="built_in">get</span>());  <span class="comment">// ③ 先写异常状态头</span></span><br><span class="line">  <span class="keyword">if</span> (_aidl_ret_status != STATUS_OK) <span class="keyword">break</span>;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (!<span class="built_in">AStatus_isOk</span>(_aidl_status.<span class="built_in">get</span>())) <span class="keyword">break</span>;      <span class="comment">// ④ 失败就结束：回程只有 status，没有返回值</span></span><br><span class="line"></span><br><span class="line">  _aidl_ret_status = ::ndk::<span class="built_in">AParcel_writeData</span>(_aidl_out, _aidl_return);  <span class="comment">// ⑤ 成功才写返回值</span></span><br><span class="line">  <span class="keyword">break</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这是所有 case 的统一四步模板：<strong>读参 → 调实现 → 写 status header → ok 才写 return</strong>。</p><p>注意第 ③ 步的语义：HAL 抛出的业务异常（如 <code>EX_ILLEGAL_ARGUMENT</code>、<code>EX_UNSUPPORTED_OPERATION</code>）是编码在 status header 里的，与 Binder 内核错误码无关，fw 侧 <code>ScopedAStatus</code> 原样还原。</p><h3 id="4-6-BpModule-openOutputStream-——-客户端桩"><a href="#4-6-BpModule-openOutputStream-——-客户端桩" class="headerlink" title="4.6 BpModule::openOutputStream —— 客户端桩"></a>4.6 BpModule::openOutputStream —— 客户端桩</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line">::<span class="function">ndk::ScopedAStatus <span class="title">BpModule::openOutputStream</span><span class="params">(<span class="type">const</span> OpenOutputStreamArguments&amp; in_args, OpenOutputStreamReturn* _aidl_return)</span> </span>&#123;</span><br><span class="line">  _aidl_ret_status = <span class="built_in">AIBinder_prepareTransaction</span>(<span class="built_in">asBinderReference</span>().<span class="built_in">get</span>(), _aidl_in.<span class="built_in">getR</span>());</span><br><span class="line">  _aidl_ret_status = ::ndk::<span class="built_in">AParcel_writeData</span>(_aidl_in.<span class="built_in">get</span>(), in_args);          <span class="comment">// ① 序列化入参</span></span><br><span class="line">  _aidl_ret_status = <span class="built_in">AIBinder_transact</span>(<span class="built_in">asBinderReference</span>().<span class="built_in">get</span>(),</span><br><span class="line">      (FIRST_CALL_TRANSACTION + <span class="number">14</span> <span class="comment">/*openOutputStream*/</span>), _aidl_in.<span class="built_in">getR</span>(), _aidl_out.<span class="built_in">getR</span>(),</span><br><span class="line">      <span class="number">0</span></span><br><span class="line">      <span class="meta">#<span class="keyword">ifdef</span> BINDER_STABILITY_SUPPORT</span></span><br><span class="line">      | <span class="keyword">static_cast</span>&lt;<span class="type">int</span>&gt;(FLAG_PRIVATE_LOCAL)                                      <span class="comment">// ② vendor 上下文私有标记</span></span><br><span class="line">      <span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line">  );</span><br><span class="line">  <span class="keyword">if</span> (_aidl_ret_status == STATUS_UNKNOWN_TRANSACTION &amp;&amp; IModule::<span class="built_in">getDefaultImpl</span>()) &#123;  <span class="comment">// ③ 本地兜底</span></span><br><span class="line">    _aidl_status = IModule::<span class="built_in">getDefaultImpl</span>()-&gt;<span class="built_in">openOutputStream</span>(in_args, _aidl_return);</span><br><span class="line">    <span class="keyword">goto</span> _aidl_status_return;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">if</span> (_aidl_ret_status != STATUS_OK) <span class="keyword">goto</span> _aidl_error;</span><br><span class="line">  _aidl_ret_status = <span class="built_in">AParcel_readStatusHeader</span>(_aidl_out.<span class="built_in">get</span>(), _aidl_status.<span class="built_in">getR</span>()); <span class="comment">// ④ 先读 status</span></span><br><span class="line">  <span class="keyword">if</span> (!<span class="built_in">AStatus_isOk</span>(_aidl_status.<span class="built_in">get</span>())) <span class="keyword">goto</span> _aidl_status_return;</span><br><span class="line">  _aidl_ret_status = ::ndk::<span class="built_in">AParcel_readData</span>(_aidl_out.<span class="built_in">get</span>(), _aidl_return);         <span class="comment">// ⑤ ok 才读返回值</span></span><br><span class="line">  ...</span><br></pre></td></tr></table></figure><p>注意第 ③ 步：如果对端（老版本服务端）不认识该事务，且本地设置了 <code>defaultImpl</code>，则直接走本地兜底实现，不再报错。</p><h2 id="五、编译产物与-Soong-模块层级"><a href="#五、编译产物与-Soong-模块层级" class="headerlink" title="五、编译产物与 Soong 模块层级"></a>五、编译产物与 Soong 模块层级</h2><p>生成的代码最终被 <code>IModule.cpp</code> 等封装到 so 里（FW&#x2F;HAL 直接链接 so 使用）。Soong 模块层级（由 <code>aidl_interface</code> 自动派生）：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">android.hardware.audio.core-V4-ndk-source      (编译生成的 19 个 cpp)</span><br><span class="line">↓ 编译</span><br><span class="line">android.hardware.audio.core-V4-ndk             → android.hardware.audio.core-V4-ndk.so   (shared)</span><br><span class="line">android.hardware.audio.core-V4-ndk_static      → .a (静态)</span><br><span class="line">android.hardware.audio.core-V4-ndk_platform    → 平台变体（无 SDK 限制）</span><br></pre></td></tr></table></figure><p>最终生成物就是 <code>android.hardware.audio.core-V4-ndk.so</code>，intermediates 路径例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">out/soong/.intermediates/.../android.hardware.audio.core-V3-ndk/android_arm64_armv8-a_sdk_shared/android.hardware.audio.core-V3-ndk.so</span><br></pre></td></tr></table></figure><p>（还有 vendor 变体、host linux_glibc 变体）</p><p>具体在 sa8797_v6_st_a 上：<code>system/lib64/</code> 和 <code>vendor/lib64/</code> 下 V1~V4 的 <code>.so</code> 全都在（<code>android.hardware.audio.core-V1-ndk.so</code> … <code>-V4-ndk.so</code>）。</p><h2 id="六、FW-HAL-两侧的引用方式"><a href="#六、FW-HAL-两侧的引用方式" class="headerlink" title="六、FW&#x2F;HAL 两侧的引用方式"></a>六、FW&#x2F;HAL 两侧的引用方式</h2><h3 id="6-1-HAL-侧（QTI-audio-hal-ar）"><a href="#6-1-HAL-侧（QTI-audio-hal-ar）" class="headerlink" title="6.1 HAL 侧（QTI audio-hal-ar）"></a>6.1 HAL 侧（QTI audio-hal-ar）</h3><p>HAL 侧类定义：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// android\vendor\qcom\opensource\audio-hal-ar\primary-hal\hal\core\include\qti-audio-core\Module.h</span></span><br><span class="line"><span class="comment">// android\vendor\qcom\opensource\audio-hal-ar\primary-hal\hal\core\include\qti-audio-core\ModulePrimary.h</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Module</span> : <span class="keyword">public</span> ::aidl::android::hardware::audio::core::BnModule,</span><br><span class="line">               <span class="keyword">public</span> std::enable_shared_from_this&lt;Module&gt; &#123;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">ModulePrimary</span> <span class="keyword">final</span> : <span class="keyword">public</span> Module &#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>构建引用：</p><figure class="highlight makefile"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">// android\vendor\qcom\opensource\audio-hal-ar\primary-hal\configs\audio-generic-modules.mk</span><br><span class="line">LATEST_ANDROID_HARDWARE_AUDIO_CORE := android.hardware.audio.core-V3-ndk</span><br><span class="line"></span><br><span class="line">// android\vendor\qcom\opensource\audio-hal-ar\primary-hal\hal\core\Android.mk</span><br><span class="line">LOCAL_SHARED_LIBRARIES := \</span><br><span class="line">    …..</span><br><span class="line">    android.hardware.common-V2-ndk \</span><br><span class="line">    <span class="variable">$(LATEST_ANDROID_MEDIA_AUDIO_COMMON_TYPES)</span> \</span><br><span class="line">    <span class="variable">$(LATEST_ANDROID_HARDWARE_AUDIO_CORE)</span> \</span><br><span class="line">    <span class="variable">$(LATEST_ANDROID_HARDWARE_AUDIO_EFFECT)</span> \</span><br><span class="line">    android.hardware.audio.core.sounddose-V1-ndk \</span><br><span class="line">    libar-pal \</span><br><span class="line">    libaudioserviceexampleimpl \</span><br><span class="line">    libaudioplatformconverter.qti \</span><br><span class="line">    qti-audio-types-aidl-V1-ndk</span><br><span class="line">……</span><br></pre></td></tr></table></figure><p>HAL 层的构建中引用了该库，其它模块也使用了该库，这里就不一一列举了。</p><p>注意：这里 QTI HAL 链接的是 V3 冻结版本，而 FW 侧（见下）链接的是 latest（V4），二者正是通过生成代码里的版本降级机制协商兼容。</p><h3 id="6-2-FW-侧（libaudiohal）"><a href="#6-2-FW-侧（libaudiohal）" class="headerlink" title="6.2 FW 侧（libaudiohal）"></a>6.2 FW 侧（libaudiohal）</h3><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br></pre></td><td class="code"><pre><span class="line">// android\frameworks\av\media\libaudiohal\impl\Android.bp</span><br><span class="line"></span><br><span class="line">cc_defaults &#123;</span><br><span class="line">    name: <span class="string">&quot;libaudiohal_aidl_default&quot;</span>,</span><br><span class="line">    defaults: [</span><br><span class="line">        <span class="string">&quot;latest_android_hardware_audio_common_ndk_shared&quot;</span>,</span><br><span class="line">        <span class="string">&quot;latest_android_hardware_audio_core_ndk_shared&quot;</span>, //这里</span><br><span class="line">        <span class="string">&quot;latest_android_hardware_audio_core_sounddose_ndk_shared&quot;</span>,</span><br><span class="line">        <span class="string">&quot;latest_android_hardware_audio_effect_ndk_shared&quot;</span>,</span><br><span class="line">        <span class="string">&quot;latest_android_media_audio_common_types_ndk_shared&quot;</span>,</span><br><span class="line">    ],</span><br><span class="line">    shared_libs: [</span><br><span class="line">        <span class="string">&quot;android.hardware.common-V2-ndk&quot;</span>,</span><br><span class="line">        <span class="string">&quot;android.hardware.common.fmq-V1-ndk&quot;</span>,</span><br><span class="line">        <span class="string">&quot;av-audio-types-aidl-ndk&quot;</span>,</span><br><span class="line">        <span class="string">&quot;com.android.media.audio-aconfig-cc&quot;</span>,</span><br><span class="line">        <span class="string">&quot;libaudio_aidl_conversion_common_cpp&quot;</span>,</span><br><span class="line">        <span class="string">&quot;libaudio_aidl_conversion_common_ndk&quot;</span>,</span><br><span class="line">        <span class="string">&quot;libaudio_aidl_conversion_common_ndk_cpp&quot;</span>,</span><br><span class="line">        <span class="string">&quot;libaudio_aidl_conversion_core_ndk&quot;</span>,</span><br><span class="line">        <span class="string">&quot;libaudio_aidl_conversion_effect_ndk&quot;</span>,</span><br><span class="line">        <span class="string">&quot;libaudioaidlcommon&quot;</span>,</span><br><span class="line">        <span class="string">&quot;libbinder_ndk&quot;</span>,</span><br><span class="line">    ],</span><br><span class="line">    header_libs: [</span><br><span class="line">        <span class="string">&quot;libaudio_system_headers&quot;</span>,</span><br><span class="line">        <span class="string">&quot;libeffectsconfig_headers&quot;</span>,</span><br><span class="line">    ],</span><br><span class="line">    cflags: [</span><br><span class="line">        <span class="string">&quot;-DBACKEND_CPP_NDK&quot;</span>,</span><br><span class="line">        <span class="string">&quot;-Wall&quot;</span>,</span><br><span class="line">        <span class="string">&quot;-Werror&quot;</span>,</span><br><span class="line">        <span class="string">&quot;-Wextra&quot;</span>,</span><br><span class="line">        <span class="string">&quot;-Wthread-safety&quot;</span>,</span><br><span class="line">    ],</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>libaudiohal_aidl_default</code> 通过 <code>cc_defaults</code> 的 <code>latest_android_hardware_audio_core_ndk_shared</code> 链接该 so。</p><p><strong>一个 so 同时含 Bp 和 Bn</strong>：<code>IModule.cpp</code> 里既有 <code>BnModule</code>（onTransact 派发）也有 <code>BpModule</code>（transact 封装），谁链接这个 so，谁就同时拿到两个方向的代码——audioserver 用 Bp 方向调用，HAL 进程用 Bn 方向服务。</p><p>服务进程本体是 <code>hal/service/Android.bp</code> 里的：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">cc_binary &#123;</span><br><span class="line">    name: <span class="string">&quot;audiohalservice.qti&quot;</span>,</span><br><span class="line">    vendor: true,</span><br><span class="line">    relative_install_path: <span class="string">&quot;hw&quot;</span>,</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>它 <code>dlopen</code> <code>libaudiocorehal.qti.so</code> 完成 <code>registerServices</code> 注册。</p><h2 id="七、端到端调用链路（以-openOutputStream-为例）"><a href="#七、端到端调用链路（以-openOutputStream-为例）" class="headerlink" title="七、端到端调用链路（以 openOutputStream 为例）"></a>七、端到端调用链路（以 openOutputStream 为例）</h2><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">audioserver                                  HAL 进程 (audiohalservice.qti + libaudiocorehal.qti.so)</span><br><span class="line">──────────────────────────────────────────   ──────────────────────────────────────────────────────</span><br><span class="line">DeviceHalAidl::openOutputStream</span><br><span class="line">  └─ BpModule::openOutputStream               binder 线程:</span><br><span class="line">       AParcel_writeData(args)                 └─ onTransact(code=14)</span><br><span class="line">       AIBinder_transact(+14) ────────────────►     AParcel_readData → in_args</span><br><span class="line">                                                     _aidl_impl-&gt;openOutputStream(args,&amp;ret)  ← 虚调到 ModulePrimary</span><br><span class="line">                                                       └─ StreamOutPrimary 创建 stream + FMQ</span><br><span class="line">                                                     writeStatusHeader + writeData(Return)</span><br><span class="line">       readStatusHeader ◄──────────────────────       (Return 内嵌 IStreamOut binder + FMQ fd)</span><br><span class="line">       readData(&amp;ret)</span><br><span class="line">  └─ 包装成 StreamHalAidl（持有 BpStreamOut + 双方 mmap 的 FMQ）</span><br></pre></td></tr></table></figure><h3 id="数据面不走-Binder"><a href="#数据面不走-Binder" class="headerlink" title="数据面不走 Binder"></a>数据面不走 Binder</h3><ul><li><strong>控制面</strong>：后续走 <code>IStreamOut</code>（AIDL，<code>start/pause/flush/drain</code> 等）；</li><li><strong>数据面</strong>：走 <code>StreamDescriptor</code>（<code>StreamDescriptor.aidl</code>）：<ul><li><code>command</code> &#x2F; <code>reply</code> FMQ：单元素队列，fw 写命令、HAL 写回复（含位置&#x2F;延迟&#x2F;xrun）；</li><li><code>audio.fmq</code>：音频数据搬运（burst 命令触发）；MMAP No IRQ 模式则用 <code>IStreamCommon.createMmapBuffer</code> 的共享内存（V4 起 <code>audio.mmap</code> 字段废弃）。</li></ul></li></ul><p>fw 侧最终把 <code>IStreamOut</code>&#x2F;<code>IStreamDescriptor</code> 封装为 <code>StreamOutHalInterface</code> 提供给 AudioFlinger mixer thread——所以你在 dumpsys&#x2F;atrace 里看到的 binder 事务主要是控制面。</p><h2 id="八、小结"><a href="#八、小结" class="headerlink" title="八、小结"></a>八、小结</h2><ul><li>Android 16 上 Audio FW 与 HAL 之间的交互使用 <strong>AIDL + FMQ</strong> 替代了 HIDL，但底层仍是 Binder；</li><li><code>IModule</code> 是 FW 与 HAL 交互的核心接口，替代 HIDL 的 <code>IDevicesFactory</code> + <code>IDevice</code>；</li><li><code>openOutputStream</code> 一次调用同时完成<strong>控制面</strong>（返回嵌套 binder <code>IStreamOut</code>）与<strong>数据面</strong>（返回 FMQ 描述符 + fd 传递 → 双方 mmap 共享内存，零拷贝）的建立；</li><li>生成代码的 <code>onTransact</code> &#x2F; <code>Bp</code> 桩是理解“FW 为什么能调到 QTI HAL”的关键，所有 HAL 实现最终都是通过虚函数挂到 <code>BnModule</code> 上的。</li></ul>]]></content>
      
      
      <categories>
          
          <category> Android </category>
          
      </categories>
      
      
        <tags>
            
            <tag> AIDL </tag>
            
            <tag> Audio HAL </tag>
            
            <tag> Binder </tag>
            
            <tag> FMQ </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
